<!DOCTYPE html>
<html lang="en">

<head>
    {{>head}}

</head>

<body>
    <!-- Main Wrapper -->
    <div class="main-wrapper">

        <!-- Header -->
        {{>header}}
        <!-- /Header -->

        <!-- Sidebar -->
        {{>admin_sidebar}}
        <!-- /Sidebar -->

        <!-- Page Wrapper -->
        <div class="page-wrapper">
            <div class="page-header">
                <div class="row align-items-center">
                    <div id="navbar-animmenu">
                        <ul class="show-dropdown main-navbar">
                            <div class="hori-selector">
                                <div class="left"></div>
                                <div class="right"></div>
                            </div>

                            <li class="active">
                                <a href="admin_add_user"><i class="fas fa-tachometer-alt"></i>User</a>
                            </li>
                            <li>
                                <a href="admin_add_project"><i class="far fa-chart-bar"></i>Project</a>
                            </li>
                            <li>
                                <a href="admin_add_client"><i class="far fa-chart-bar"></i>Client</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="content container-fluid">
                <div class="page-header">
                    <div class="row align-items-center">
                        <div class="col">
                            <h3 class="page-title">Settings</h3>
                            <ul class="breadcrumb">
                                <li class="breadcrumb-item">Home</li>
                                <li class="breadcrumb-item active">Settings</li>
                                <li class="breadcrumb-item active">User</li>
                            </ul>
                        </div>
                    </div>
                </div>



                {{>add_user}}
                {{>admin_edit_user_pop_pop}}
            </div>
        </div>


    </div>


    </div>
    <!-- /Page Wrapper -->

    </div>
    <!-- /Main Wrapper -->

    {{>js}}

</body>
<script>
    $(document).ready(function () {
        $('#liadmin_add_user').addClass('active');
    });

    data()
    function adduser() {
        event.preventDefault();

        document.getElementById("error").innerHTML = "";
        var email = document.getElementById("email").value
        var password = document.getElementById("password").value
        var name = document.getElementById("name").value
        var role = document.getElementById("role").value

        var minNumberofChars = 7;
        var maxNumberofChars = 15;
        var regularExpression = new RegExp("^(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])(?=.*[^A-Za-z0-9_])");
        var emailvalid = new RegExp("^[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+@[a-zA-Z0-9-]+(?:\.[a-zA-Z0-9-]+)*$");

        if (document.getElementById("email").value.trim() == "" && document.getElementById("password").value.trim() == "" && document.getElementById("name").value.trim() == "") {
            document.getElementById("error").innerHTML = "Enter Email Id, Password and Name";
        } else if (document.getElementById("email").value.trim() == "" && document.getElementById("password").value.trim() == "") {
            document.getElementById("error").innerHTML = "Enter Email Id and Password ";
        } else if (document.getElementById("email").value.trim() == "") {
            document.getElementById("error").innerHTML = "Enter Email Id ";
        } else if (document.getElementById("password").value.trim() == "") {
            document.getElementById("error").innerHTML = "Enter Password ";
        } else if (password.length < minNumberofChars || password.length > maxNumberofChars) {
            document.getElementById("error").innerHTML = "Password Length minimum 8 character to maximum 15 character";
        } else if (document.getElementById("name").value.trim() == "") {
            document.getElementById("error").innerHTML = "Enter Name ";
        } else {
            if (!emailvalid.test(email)) {
                document.getElementById("error").innerHTML = "Enter Valid email address!";
            }
            else if (!regularExpression.test(password)) {
                document.getElementById("error").innerHTML = "password should contain at least one number and one special character";

            } else {

                var data = {
                    email: email,
                    password: password,
                    name: name,
                    role: role
                }
                var myHeaders = {
                    "Content-Type": "application/json",
                };
                var formdata = new FormData();
                formdata.append("email", email);
                formdata.append("password", password);
                formdata.append("name", name);
                formdata.append("role", role);
                //var raw = JSON.stringify(data);
                var requestOptions = {
                    method: 'POST',
                    body: formdata,
                    redirect: 'follow'
                };
                fetch("http://127.0.0.1:3647/api/users/register", requestOptions)
                    .then(response => response.json())
                    .then(result => {
                        if (!result.error) {
                            Swal.fire({
                                //position: 'top-end',
                                icon: 'success',
                                title: 'Add Sucessful',
                                showConfirmButton: false,
                                timer: 3000
                            })
                            
                            setTimeout(function () {
                                window.location.reload();
                            }, 3000);
                            
                            

                        } else {
                            console.log(result.message)
                            document.getElementById("error").innerHTML = result.message
                        }

                    })
                    .catch(error => console.log('error', error));
            }
        }
    }

    function list() {
        //setTimeout(function () {
        //    window.location.reload();
        //}, 3000);

        var email = document.getElementById("email").value
        var name = document.getElementById("name").value
        var role = document.getElementById("role").value
        var table = document.getElementById('tbd');
        var template = `
<tr>
                        <td >${email}</td>
                        <td >${name}</td>
                        <td >${role}</td>
                       
                        <td class="text-right">
								<div class="dropdown dropdown-action">
									<a href="#" class="action-icon dropdown-toggle" data-toggle="dropdown"
										aria-expanded="false"><i class="material-icons">more_vert</i></a>
									<div class="dropdown-menu dropdown-menu-right">
									<a onclick="{edit('${email}','${name}','${role}','${id}')}" class="dropdown-item" href="#" data-toggle="modal" data-target="#admin_edit_user"><i onclick="edit(this)" class="fa fa-pencil m-r-5"></i> Edit</a>
										<a onclick="{Delete('${id}')}"  class="dropdown-item" href="#" data-toggle="modal"
											data-target="#delete_department"><i onclick="Delete(this)" class="fa fa-trash-o m-r-5"></i>
											Delete</a>
									</div>
								</div>
							</td>
                    </tr>
        `;
        table.innerHTML += template;
        document.getElementById("email").value = "";
        document.getElementById("password").value = "";
        document.getElementById("name").value = "";
        document.getElementById("role").value = "";

    }


    function data() {

        var requestOptions = {
            method: 'GET',
            redirect: 'follow'
        };

        fetch("http://127.0.0.1:3647/api/users", requestOptions)
            .then(response => response.json())
            .then(result => {
                //const Data2 = result.data
                //const Data = result.data[0].email
                //const Data1 = result.data[0].password
                //console.log(Data)
                //console.log(Data1)
                //console.log(Data2)
                var table = document.getElementById('tbd');
                table.innerHTML = "";

                for (i = 0; i <= result.data.length; i++) {
                    var email = result.data[i].email
                    var name = result.data[i].name
                    var role = result.data[i].role
                    var id = result.data[i]._id
                    //var password = result.data[i].password
                    var template = `
                    <tr>
                        <td >${email}</td>
                        <td >${name}</td>
                        <td >${role}</td>
                        <td class="text-right">
								<div class="dropdown dropdown-action">
									<a href="#" class="action-icon dropdown-toggle" data-toggle="dropdown"
										aria-expanded="false"><i class="material-icons">more_vert</i></a>
									<div class="dropdown-menu dropdown-menu-right">
										<a onclick="{edit('${email}','${name}','${role}','${id}')}" class="dropdown-item" href="#" data-toggle="modal" data-target="#admin_edit_user"><i onclick="edit(this)" class="fa fa-pencil m-r-5"></i> Edit</a>
										<a onclick="{Delete('${id}')}"  class="dropdown-item" href="#" data-toggle="modal"
											data-target="#delete_department"><i onclick="Delete(this)" class="fa fa-trash-o m-r-5"></i>
											Delete</a>
									</div>
								</div>
							</td>
                    </tr>`;
                    table.innerHTML += template;
                }


            })
            .catch(error => console.log('error', error));
        //location.reload();
    }

    function Delete(id) {
        Swal.fire({
            title: 'Are you sure?',
            text: "You won't be able to revert this!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Yes, delete it!'
        }).then((result) => {
            if (result.isConfirmed) {
                Swal.fire(
                    'Deleted!',
                    'Your file has been deleted.',
                    'success'
                )
                var requestOptions = {
                    method: 'DELETE',
                    redirect: 'follow'
                };

                fetch(`http://127.0.0.1:3647/api/users/${id}`, requestOptions)
                    .then(response => response.json())
                    .then(result => {
                        if (!result.error) {
                            data()
                        } else {
                            document.getElementById("error").innerHTML = result.message
                        }
                    })
                    .catch(error => console.log('error', error));
            }
        })
    }

    function edit(email, name, role, id) {
        // var Email = email
        var Name = name
        var Role = role
        var select = document.getElementById("editrole");
        console.log(Role);


        // document.getElementById("editemail").value = Email;
        document.getElementById("editname").value = Name;
        select.value = Role;
        triggerChange(select)
        document.getElementById("editid").value = id;

    }

    function triggerChange(element) {
        let changeEvent = new Event("change");
        element.dispatchEvent(changeEvent);
    }


    function updateuser() {

        event.preventDefault();
        document.getElementById("error1").innerHTML = "";
        //var Email_Id = document.getElementById("editemail").value
        if (document.getElementById("editname").value.trim() == "") {
            document.getElementById("error1").innerHTML = "Enter Name";
        } else {
            var Name = document.getElementById("editname").value
            var Role = document.getElementById("editrole").value
            var _Id = document.getElementById("editid").value


            var myHeaders = new Headers();
            myHeaders.append("Content-Type", "application/x-www-form-urlencoded");

            var urlencoded = new URLSearchParams();
            //urlencoded.append("email", Email_Id);
            urlencoded.append("name", Name);
            urlencoded.append("role", Role);

            var requestOptions = {
                method: 'PUT',
                headers: myHeaders,
                body: urlencoded,
                redirect: 'follow'
            };

            fetch(`http://127.0.0.1:3647/api/users/${_Id}`, requestOptions)
                .then(response => response.json())
                .then(result => {
                    if (!result.error) {
                        Swal.fire({
                            //position: 'top-end',
                            icon: 'success',
                            title: 'Update Successful',
                            showConfirmButton: false,
                            timer: 3000
                        })
                        setTimeout(function () {
                            window.location.reload();
                        }, 3000);
                    } else {
                        document.getElementById("error1").innerHTML = result.message
                    }
                })
                .catch(error => console.log('error', error));
        }

    }
</script>

</html>